/******/ (() => { // webpackBootstrap
// This entry needs to be wrapped in an IIFE because it needs to be isolated against other entry modules.
(() => {
  function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
  function _superPropGet(t, o, e, r) { var p = _get(_getPrototypeOf(1 & r ? t.prototype : t), o, e); return 2 & r && "function" == typeof p ? function (t) { return p.apply(e, t); } : p; }
  function _get() { return _get = "undefined" != typeof Reflect && Reflect.get ? Reflect.get.bind() : function (e, t, r) { var p = _superPropBase(e, t); if (p) { var n = Object.getOwnPropertyDescriptor(p, t); return n.get ? n.get.call(arguments.length < 3 ? e : r) : n.value; } }, _get.apply(null, arguments); }
  function _superPropBase(t, o) { for (; !{}.hasOwnProperty.call(t, o) && null !== (t = _getPrototypeOf(t));); return t; }
  function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
  function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
  function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
  function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
  function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
  function _callSuper(t, o, e) { return o = _getPrototypeOf(o), _possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], _getPrototypeOf(t).constructor) : o.apply(t, e)); }
  function _possibleConstructorReturn(t, e) { if (e && ("object" == _typeof(e) || "function" == typeof e)) return e; if (void 0 !== e) throw new TypeError("Derived constructors may only return object or undefined"); return _assertThisInitialized(t); }
  function _assertThisInitialized(e) { if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); return e; }
  function _inherits(t, e) { if ("function" != typeof e && null !== e) throw new TypeError("Super expression must either be null or a function"); t.prototype = Object.create(e && e.prototype, { constructor: { value: t, writable: !0, configurable: !0 } }), Object.defineProperty(t, "prototype", { writable: !1 }), e && _setPrototypeOf(t, e); }
  function _wrapNativeSuper(t) { var r = "function" == typeof Map ? new Map() : void 0; return _wrapNativeSuper = function _wrapNativeSuper(t) { if (null === t || !_isNativeFunction(t)) return t; if ("function" != typeof t) throw new TypeError("Super expression must either be null or a function"); if (void 0 !== r) { if (r.has(t)) return r.get(t); r.set(t, Wrapper); } function Wrapper() { return _construct(t, arguments, _getPrototypeOf(this).constructor); } return Wrapper.prototype = Object.create(t.prototype, { constructor: { value: Wrapper, enumerable: !1, writable: !0, configurable: !0 } }), _setPrototypeOf(Wrapper, t); }, _wrapNativeSuper(t); }
  function _construct(t, e, r) { if (_isNativeReflectConstruct()) return Reflect.construct.apply(null, arguments); var o = [null]; o.push.apply(o, e); var p = new (t.bind.apply(t, o))(); return r && _setPrototypeOf(p, r.prototype), p; }
  function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
  function _isNativeFunction(t) { try { return -1 !== Function.toString.call(t).indexOf("[native code]"); } catch (n) { return "function" == typeof t; } }
  function _setPrototypeOf(t, e) { return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function (t, e) { return t.__proto__ = e, t; }, _setPrototypeOf(t, e); }
  function _getPrototypeOf(t) { return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function (t) { return t.__proto__ || Object.getPrototypeOf(t); }, _getPrototypeOf(t); }
  var SearchForm = /*#__PURE__*/function (_HTMLElement) {
    function SearchForm() {
      var _this;
      _classCallCheck(this, SearchForm);
      _this = _callSuper(this, SearchForm);
      var deviceWidth = window.innerWidth;
      if (deviceWidth < 1024) {
        _this.input = document.querySelector('[data-p-search-input-mobile]');
      } else {
        _this.input = document.querySelector('[data-p-search-input]');
      }
      _this.form = document.querySelector('[data-search-form]');
      _this.resetButton = _this.querySelector('button[type="reset"]');
      if (_this.input) {
        _this.form.addEventListener("reset", _this.onFormReset.bind(_this));
        _this.input.addEventListener("input", debounce(function (event) {
          _this.onChange(event);
        }, 300).bind(_this));
      }
      return _this;
    }
    _inherits(SearchForm, _HTMLElement);
    return _createClass(SearchForm, [{
      key: "toggleResetButton",
      value: function toggleResetButton() {
        var resetIsHidden = this.resetButton.classList.contains("hidden");
        if (this.input.value.length > 0 && resetIsHidden) {
          this.resetButton.classList.remove("hidden");
        } else if (this.input.value.length === 0 && !resetIsHidden) {
          this.resetButton.classList.add("hidden");
        }
      }
    }, {
      key: "onChange",
      value: function onChange() {
        this.toggleResetButton();
      }
    }, {
      key: "shouldResetForm",
      value: function shouldResetForm() {
        return !document.querySelector('[aria-selected="true"] a');
      }
    }, {
      key: "onFormReset",
      value: function onFormReset(event) {
        // Prevent default so the form reset doesn't set the value gotten from the url on page load
        event.preventDefault();
        // Don't reset if the user has selected an element on the predictive search dropdown
        if (this.shouldResetForm()) {
          this.input.value = "";
          this.input.focus();
          this.toggleResetButton();
        }
      }
    }]);
  }(/*#__PURE__*/_wrapNativeSuper(HTMLElement));
  if (!customElements.get("search-form")) {
    customElements.define("search-form", SearchForm);
  }
  var PredictiveSearch = /*#__PURE__*/function (_SearchForm) {
    function PredictiveSearch() {
      var _this2;
      _classCallCheck(this, PredictiveSearch);
      _this2 = _callSuper(this, PredictiveSearch);
      _this2.cachedResults = {};
      _this2.predictiveSearchResults = _this2.querySelector("[data-predictive-search]");
      _this2.allPredictiveSearchInstances = document.querySelectorAll("predictive-search");
      _this2.isOpen = false;
      _this2.abortController = new AbortController();
      _this2.searchTerm = "";
      _this2.clearSearch = _this2.querySelector('.predictive-clear');
      var deviceWidth = window.innerWidth;
      _this2.closeSearch = document.querySelectorAll('[data-search-close-mobile], [data-search-close]');
      _this2.overlay = document.querySelector("[data-desktop-overlay]");
      _this2.setupEventListeners();
      return _this2;
    }
    _inherits(PredictiveSearch, _SearchForm);
    return _createClass(PredictiveSearch, [{
      key: "setupEventListeners",
      value: function setupEventListeners() {
        var _this3 = this;
        this.form.addEventListener("submit", this.onFormSubmit.bind(this));
        this.input.addEventListener("focus", this.onFocus.bind(this));
        this.addEventListener("focusout", this.onFocusOut.bind(this));
        this.addEventListener("keyup", this.onKeyup.bind(this));
        this.addEventListener("keydown", this.onKeydown.bind(this));
        this.clearSearch.addEventListener("click", this.close.bind(this));
        this.clearSearch.addEventListener("click", function () {
          document.querySelector(".popular-searches").classList.add('active');
        });
        this.closeSearch.forEach(function (closer) {
          closer.addEventListener("click", function () {
            console.log("close1111");
            _this3.input.value = "";
            document.querySelector(".predictive-search__container").classList.remove("active");
            document.querySelector("[data-p-search-input]").classList.remove("active");
            document.querySelector("[data-search-close]").classList.remove("active");
            document.querySelector("[data-desktop-nav]").classList.remove("d-none");
            document.querySelector("[data-desktop-overlay]").classList.remove("desktop-overlay--active");
            document.querySelector("html").classList.remove("no-scroll");
          });
        });
      }
    }, {
      key: "getQuery",
      value: function getQuery() {
        return this.input.value.trim();
      }
    }, {
      key: "onChange",
      value: function onChange() {
        _superPropGet(PredictiveSearch, "onChange", this, 3)([]);
        var popularSearches = document.querySelector(".popular-searches");
        var newSearchTerm = this.getQuery();
        if (!this.searchTerm || !newSearchTerm.startsWith(this.searchTerm)) {
          var _this$querySelector;
          // Remove the results when they are no longer relevant for the new search term
          // so they don't show up when the dropdown opens again
          (_this$querySelector = this.querySelector("#predictive-search-results-groups-wrapper")) === null || _this$querySelector === void 0 || _this$querySelector.remove();
        }
  
        // Update the term asap, don't wait for the predictive search query to finish loading
        //this.updateSearchForTerm(this.searchTerm, newSearchTerm);
        this.searchTerm = newSearchTerm;
        if (!this.searchTerm.length) {
          console.log("here---");
          this.close(true);
          popularSearches.classList.add('active');
          return;
        } else {
          popularSearches.classList.remove('active');
        }

        console.log("search term", this.searchTerm);
        
        this.getSearchResults(this.searchTerm);
      }
    }, {
      key: "onFormSubmit",
      value: function onFormSubmit(event) {
        console.log("Submit form event", event);
        if (!this.getQuery().length || this.querySelector('[aria-selected="true"] a')) event.preventDefault();
      }
    }, {
      key: "onFormReset",
      value: function onFormReset(event) {
        _superPropGet(PredictiveSearch, "onFormReset", this, 3)([event]);
        if (_superPropGet(PredictiveSearch, "shouldResetForm", this, 3)([])) {
          this.searchTerm = "";
          this.abortController.abort();
          this.abortController = new AbortController();
          this.closeResults(true);
        }
      }
    }, {
      key: "onFocus",
      value: function onFocus() {
        var currentSearchTerm = this.getQuery();
        if (!currentSearchTerm.length) return;
        if (this.searchTerm !== currentSearchTerm) {
          // Search term was changed from other search input, treat it as a user change
          this.onChange();
        } else if (this.getAttribute("results") === "true") {
          this.open();
        } else {
          this.getSearchResults(this.searchTerm);
        }
      }
    }, {
      key: "onFocusOut",
      value: function onFocusOut() {
        var _this4 = this;
        console.log("here---???");
        setTimeout(function () {
          if (!_this4.contains(document.activeElement)) _this4.close();
        });
      }
    }, {
      key: "onKeyup",
      value: function onKeyup(event) {
        if (!this.getQuery().length) {
          console.log("here---xxx");
          this.close(true);
        }
        event.preventDefault();
        switch (event.code) {
          case "ArrowUp":
            this.switchOption("up");
            break;
          case "ArrowDown":
            this.switchOption("down");
            break;
          case "Enter":
            this.selectOption();
            break;
        }
      }
    }, {
      key: "onKeydown",
      value: function onKeydown(event) {
        // Prevent the cursor from moving in the input when using the up and down arrow keys
        if (event.code === "ArrowUp" || event.code === "ArrowDown") {
          event.preventDefault();
        }
      }
    }, {
      key: "updateSearchForTerm",
      value: function updateSearchForTerm(previousTerm, newTerm) {
        var searchForTextElement = this.querySelector("[data-predictive-search-search-for-text]");
        var currentButtonText = searchForTextElement === null || searchForTextElement === void 0 ? void 0 : searchForTextElement.innerText;
        if (currentButtonText) {

          if (currentButtonText.match(new RegExp(previousTerm, "g")).length > 1) {
            // The new term matches part of the button text and not just the search term, do not replace to avoid mistakes
            return;
          }
          var newButtonText = currentButtonText.replace(previousTerm, newTerm);
          searchForTextElement.innerText = newButtonText;
        }
      }
    }, {
      key: "switchOption",
      value: function switchOption(direction) {
        if (!this.getAttribute("open")) return;
        var moveUp = direction === "up";
        var selectedElement = this.querySelector('[aria-selected="true"]');
  
        // Filter out hidden elements (duplicated page and article resources) thanks
        // to this https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement/offsetParent
        var allVisibleElements = Array.from(this.querySelectorAll("li, button.predictive-search__item")).filter(function (element) {
          return element.offsetParent !== null;
        });
        var activeElementIndex = 0;
        if (moveUp && !selectedElement) return;
        var selectedElementIndex = -1;
        var i = 0;
        while (selectedElementIndex === -1 && i <= allVisibleElements.length) {
          if (allVisibleElements[i] === selectedElement) {
            selectedElementIndex = i;
          }
          i++;
        }
        this.statusElement.textContent = "";
        if (!moveUp && selectedElement) {
          activeElementIndex = selectedElementIndex === allVisibleElements.length - 1 ? 0 : selectedElementIndex + 1;
        } else if (moveUp) {
          activeElementIndex = selectedElementIndex === 0 ? allVisibleElements.length - 1 : selectedElementIndex - 1;
        }
        if (activeElementIndex === selectedElementIndex) return;
        var activeElement = allVisibleElements[activeElementIndex];
        activeElement.setAttribute("aria-selected", true);
        if (selectedElement) selectedElement.setAttribute("aria-selected", false);
        this.input.setAttribute("aria-activedescendant", activeElement.id);
      }
    }, {
      key: "selectOption",
      value: function selectOption() {
        var selectedOption = this.querySelector('[aria-selected="true"] a, button[aria-selected="true"]');
        if (selectedOption) selectedOption.click();
      }
    }, {
      key: "getSearchResults",
      value: function getSearchResults(searchTerm) {
        var _this5 = this;
        console.log("getting search results");
        var queryKey = searchTerm.replace(" ", "-").toLowerCase();
        this.setLiveRegionLoadingState();
        if (this.cachedResults[queryKey]) {
          this.renderSearchResults(this.cachedResults[queryKey]);
          return;
        }
        
        fetch(`${routes.predictive_search_url}?q=${encodeURIComponent(searchTerm)}&section_id=predictive-search&resources[options][fields]=title,tag,product_type,variants.title,vendor`, {
          signal: this.abortController.signal
        }).then(function (response) {
          if (!response.ok) {
            var error = new Error(response.status);
            console.log("here---yyyy");
            _this5.close();
            throw error;
          }
          return response.text();
        }).then(function (text) {
          var resultsMarkup = new DOMParser().parseFromString(text, "text/html").querySelector("#shopify-section-predictive-search").innerHTML;
          // Save bandwidth keeping the cache in all instances synced
          _this5.allPredictiveSearchInstances.forEach(function (predictiveSearchInstance) {
            predictiveSearchInstance.cachedResults[queryKey] = resultsMarkup;
          });
          _this5.renderSearchResults(resultsMarkup);
        })["catch"](function (error) {
          if ((error === null || error === void 0 ? void 0 : error.code) === 20) {
            // Code 20 means the call was aborted
            return;
          }
          console.log("here---zzz");
          _this5.close();
          throw error;
        });
      }
    }, {
      key: "setLiveRegionLoadingState",
      value: function setLiveRegionLoadingState() {
        this.statusElement = this.statusElement || this.querySelector(".predictive-search-status");
        this.loadingText = this.loadingText || this.getAttribute("data-loading-text");
        this.setLiveRegionText(this.loadingText);
        this.setAttribute("loading", true);
      }
    }, {
      key: "setLiveRegionText",
      value: function setLiveRegionText(statusText) {
        var _this6 = this;
        this.statusElement.setAttribute("aria-hidden", "false");
        this.statusElement.textContent = statusText;
        setTimeout(function () {
          _this6.statusElement.setAttribute("aria-hidden", "true");
        }, 1000);
      }
    }, {
      key: "renderSearchResults",
      value: function renderSearchResults(resultsMarkup) {
        this.predictiveSearchResults.innerHTML = resultsMarkup;
        this.setAttribute("results", true);
        this.setLiveRegionResults();
        this.open();
      }
    }, {
      key: "setLiveRegionResults",
      value: function setLiveRegionResults() {
        this.removeAttribute("loading");
        this.setLiveRegionText(this.querySelector("[data-predictive-search-live-region-count-value]").textContent);
      }
    }, {
      key: "getResultsMaxHeight",
      value: function getResultsMaxHeight() {
        this.resultsMaxHeight = window.innerHeight - document.querySelector(".header").getBoundingClientRect().bottom;
        return this.resultsMaxHeight;
      }
    }, {
      key: "open",
      value: function open() {
        this.predictiveSearchResults.style.maxHeight = this.resultsMaxHeight || "".concat(this.getResultsMaxHeight(), "px");
        this.setAttribute("open", true);
        this.input.setAttribute("aria-expanded", true);
        this.isOpen = true;
      }
    }, {
      key: "close",
      value: function close() {
        var clearSearchTerm = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
        console.log("close2222");
        this.closeResults(clearSearchTerm);
        this.isOpen = false;
      }
    }, {
      key: "closeResults",
      value: function closeResults() {
        var clearSearchTerm = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
        if (clearSearchTerm) {
          this.input.value = "";
          this.removeAttribute("results");
        }
        var selected = this.querySelector('[aria-selected="true"]');
        if (selected) selected.setAttribute("aria-selected", false);
        this.input.setAttribute("aria-activedescendant", "");
        this.removeAttribute("loading");
        this.removeAttribute("open");
        this.input.setAttribute("aria-expanded", false);
        this.resultsMaxHeight = false;
        this.predictiveSearchResults.removeAttribute("style");
      }
    }]);
  }(SearchForm);
  if (!customElements.get("predictive-search")) {
    customElements.define("predictive-search", PredictiveSearch);
  }
  })();
  
  // This entry needs to be wrapped in an IIFE because it needs to be in strict mode.
  (() => {
  "use strict";
  // extracted by mini-css-extract-plugin
  
  })();
  
  /******/ })()
  ;