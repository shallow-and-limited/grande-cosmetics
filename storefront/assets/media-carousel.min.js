/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
// This entry needs to be wrapped in an IIFE because it needs to be isolated against other entry modules.
(() => {

  ;// ./node_modules/selectricity/index.js
  const nodeListPrototype = {
    execute(fn) {
      if (this instanceof Element || this instanceof HTMLDocument) {
        fn(this);
      } else {
        this.forEach((node, index) => {
          fn(node, index);
        });
      }
    },
    listen(eventName, callback, dynamic) {
      const callbackWithNode = ($node, index) => (e) =>
        callback({ $node, index, e });
  
      const events = eventName.split(' ');
  
      this.execute((node, index) => {
        const extendedNode = Object.assign(node, nodeListPrototype);
        if (dynamic) {
          events.forEach(event => {
            document.addEventListener(event, (e) => {
              const $intendedNode = e.target.closest(this.selectedWith);
              if ($intendedNode) {
                callbackWithNode(extendedNode, index)(e);
              }
            });
          });
        } else {
          events.forEach(event => {
            extendedNode.addEventListener(event, (e) =>
              callbackWithNode(extendedNode, index)(e)
            );
          })
        }
      });
    },
    modifyClass(method, className) {
      this.execute((node) => node.classList[method](className));
    },
    addClass(className) {
      this.execute((node) => node.classList.add(className));
    },
    removeClass(className) {
      this.execute((node) => node.classList.remove(className));
    },
    toggleClass(className) {
      this.execute((node) => node.classList.toggle(className));
    },
    hasClass(className) {
      if (!this.classList) return false;
      const classes = [...this.classList];
      return classes.includes(className);
    },
    addAttr(attribute, value = '') {
      this.execute((node) => node.setAttribute(attribute, value));
    },
    removeAttr(attribute) {
      this.execute((node) => node.removeAttribute(attribute));
    },
    toggleAttr(attribute, value = '') {
      this.execute((node) => {
        if (node.hasAttribute(attribute)) {
          node.removeAttribute(attribute);
        } else {
          node.setAttribute(attribute, value);
        }
      });
    }
  };
  
  function selectricity_select(selector, parent = document) {
    /*
      Selector can either be a node or string.
      If string, select the appropriate nodes within the parent (default is document),
      otherwise continue
    */
    const object =
      typeof selector === 'string' ? parent.querySelectorAll(selector) : selector;
  
    /*
      For each node, attach the custom helper functions from the nodeListPrototype above
    */
    const mappedObject = [...object].map((item) =>
      Object.assign(item, nodeListPrototype, { selectedWith: selector })
    );
  
    /*
      If the node list contains only a single node, return the node itself, otherwise return the array of nodes
    */
    const selectObject =
      mappedObject && mappedObject.length === 1 ? mappedObject[0] : mappedObject;
  
    /*
      Attach the custom helper functions to the returned object itself.
      (Useful for executing a custom function on every item in the object (modifyClass))
    */
    const extendedObject = Object.assign(selectObject, nodeListPrototype);
  
    return extendedObject;
  }
  
  
  
  ;// ./components/media-carousel/media-carousel.js
  function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
  function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
  function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
  function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
  function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
  function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
  function _callSuper(t, o, e) { return o = _getPrototypeOf(o), _possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], _getPrototypeOf(t).constructor) : o.apply(t, e)); }
  function _possibleConstructorReturn(t, e) { if (e && ("object" == _typeof(e) || "function" == typeof e)) return e; if (void 0 !== e) throw new TypeError("Derived constructors may only return object or undefined"); return _assertThisInitialized(t); }
  function _assertThisInitialized(e) { if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); return e; }
  function _inherits(t, e) { if ("function" != typeof e && null !== e) throw new TypeError("Super expression must either be null or a function"); t.prototype = Object.create(e && e.prototype, { constructor: { value: t, writable: !0, configurable: !0 } }), Object.defineProperty(t, "prototype", { writable: !1 }), e && _setPrototypeOf(t, e); }
  function _wrapNativeSuper(t) { var r = "function" == typeof Map ? new Map() : void 0; return _wrapNativeSuper = function _wrapNativeSuper(t) { if (null === t || !_isNativeFunction(t)) return t; if ("function" != typeof t) throw new TypeError("Super expression must either be null or a function"); if (void 0 !== r) { if (r.has(t)) return r.get(t); r.set(t, Wrapper); } function Wrapper() { return _construct(t, arguments, _getPrototypeOf(this).constructor); } return Wrapper.prototype = Object.create(t.prototype, { constructor: { value: Wrapper, enumerable: !1, writable: !0, configurable: !0 } }), _setPrototypeOf(Wrapper, t); }, _wrapNativeSuper(t); }
  function _construct(t, e, r) { if (_isNativeReflectConstruct()) return Reflect.construct.apply(null, arguments); var o = [null]; o.push.apply(o, e); var p = new (t.bind.apply(t, o))(); return r && _setPrototypeOf(p, r.prototype), p; }
  function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
  function _isNativeFunction(t) { try { return -1 !== Function.toString.call(t).indexOf("[native code]"); } catch (n) { return "function" == typeof t; } }
  function _setPrototypeOf(t, e) { return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function (t, e) { return t.__proto__ = e, t; }, _setPrototypeOf(t, e); }
  function _getPrototypeOf(t) { return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function (t) { return t.__proto__ || Object.getPrototypeOf(t); }, _getPrototypeOf(t); }
  
  var MediaCarousel = /*#__PURE__*/function (_HTMLElement) {
    function MediaCarousel() {
      var _this;
      _classCallCheck(this, MediaCarousel);
      _this = _callSuper(this, MediaCarousel);
      window.loadSwiper.then(function (_ref) {
        var Swiper = _ref.Swiper,
          Scrollbar = _ref.Scrollbar,
          Navigation = _ref.Navigation,
          EffectFade = _ref.EffectFade,
          Pagination = _ref.Pagination,
          Thumbs = _ref.Thumbs,
          FreeMode = _ref.FreeMode,
          Mousewheel = _ref.Mousewheel;
        _this.Swiper = Swiper;
        _this.Scrollbar = Scrollbar;
        _this.Navigation = Navigation;
        _this.EffectFade = EffectFade;
        _this.Pagination = Pagination;
        _this.Thumbs = Thumbs;
        _this.FreeMode = FreeMode;
        _this.Mousewheel = Mousewheel;
        _this.initCarousel();
      });
      return _this;
    }
    _inherits(MediaCarousel, _HTMLElement);
    return _createClass(MediaCarousel, [{
      key: "initCarousel",
      value: function initCarousel() {
        if (this.swiper) return;
        if (this.thumbnails) return;
        var slider = selectricity_select('[data-gallery-swiper]', this);
        var thumbsSwiper = selectricity_select('[data-thumbs-swiper]', this);
        var swiperParamsThumbs = {
          modules: [this.Mousewheel, this.FreeMode],
          slidesPerView: 'auto',
          spaceBetween: 5,
          mousewheel: {
            releaseOnEdges: true,
            enabled: true
          },
          freeMode: true,
          breakpoints: {
            1024: {
              direction: 'vertical'
            }
          }
        };
        this.thumbnails = new this.Swiper(thumbsSwiper, swiperParamsThumbs);
        var swiperParams = {
          modules: [this.Navigation, this.EffectFade, this.Thumbs],
          slidesPerView: 1,
          effect: "fade",
          fadeEffect: {
            crossFade: true
          },
          thumbs: {
            swiper: this.thumbnails
          },
          navigation: {
            nextEl: selectricity_select('[data-next]', this),
            prevEl: selectricity_select('[data-previous]', this)
          }
        };
        this.swiper = new this.Swiper(slider, swiperParams);
        this.addEventListeners();
        this.initialSlideToCurrentVariant();
      }
    }, {
      key: "toggleSwiperClasses",
      value: function toggleSwiperClasses(add) {
        selectricity_select('[data-swiper-class]', this.parentNode).forEach(function (el) {
          el.classList.toggle(el.dataset.swiperClass, add);
        });
      }
    }, {
      key: "addEventListeners",
      value: function addEventListeners() {
        var _this2 = this;
        var variantSwatches = document.querySelectorAll('.tabs.tabs--pdp input');
        if (variantSwatches) {
          variantSwatches.forEach(function (variant) {
            variant.addEventListener('click', function () {
              _this2.slideToCurrentVariant();
            });
          });
        }
        var colorSwatches = document.querySelectorAll('.product-form__swatch[data-color]');
        if (colorSwatches) {
          colorSwatches.forEach(function (color) {
            color.addEventListener('click', function () {
              var colorTarget = color.getAttribute('data-swatch');
              _this2.slideToCurrentColor(colorTarget);
            });
          });
        }
      }
    }, {
      key: "slideToCurrentVariant",
      value: function slideToCurrentVariant() {
        var _this3 = this;
        var selectedSwatch = document.querySelector('.tabs.tabs--pdp input:checked');
        var variantTitle = selectedSwatch.getAttribute('data-variant-title');
        var variantImageId = selectedSwatch.getAttribute('data-variant-image');
        var variantId = selectedSwatch.getAttribute('id');
  
        // Append variant ID to URL without triggering a reload
        if (variantId) {
          var url = new URL(window.location.href);
          url.searchParams.set('variant', variantId);
          window.history.replaceState(null, '', url.toString());
        }
  
        // The carousel slides display the images alt tag as `data-variant-title`
        // if the alt tag matches the selected variant, slide to that image.
        if (variantImageId) {
          this.swiper.slides.forEach(function (slide) {
            var imageId = slide.getAttribute('data-media-id');
            
            if (variantImageId === imageId) {
              _this3.swiper.slideTo(parseInt(slide.getAttribute('data-index')) - 1);
            }
          });
        }
      }
    }, {
      key: "slideToCurrentColor",
      value: function slideToCurrentColor(colorTarget) {
        var _this4 = this;
        this.swiper.slides.forEach(function (slide) {
          var imageColor = slide.getAttribute('data-variant-title');
          if (colorTarget === imageColor) {
            var slideIndex = slide.getAttribute('data-index');
            slideIndex--;
            _this4.swiper.slideTo(slideIndex);
          }
        });
      }
    }, {
      key: "initialSlideToCurrentVariant",
      value: function initialSlideToCurrentVariant() {
        var _this5 = this;
        var selectedColorElement = document.querySelector('[data-selected-color]');
        var selectedOptionElement = document.querySelector('[data-selected-option]');
        if (selectedColorElement && selectedColorElement.textContent.trim()) {
          var colorTarget = selectedColorElement.textContent.trim();
          this.slideToCurrentColor(colorTarget);
        } else if (selectedOptionElement && selectedOptionElement.textContent.trim()) {
          var variantTitle = selectedOptionElement.textContent.trim();
          this.swiper.slides.forEach(function (slide) {
            var imageAlt = slide.getAttribute('data-variant-title');
            if (variantTitle === imageAlt) {
              _this5.swiper.slideTo(slide.getAttribute('data-index'));
            }
          });
        }
      }
    }]);
  }(/*#__PURE__*/_wrapNativeSuper(HTMLElement));
  if (!customElements.get("media-carousel")) {
    customElements.define("media-carousel", MediaCarousel);
  }
  })();
  
  // This entry needs to be wrapped in an IIFE because it needs to be isolated against other entry modules.
  (() => {
  // extracted by mini-css-extract-plugin
  
  })();
  
  /******/ })()
  ;